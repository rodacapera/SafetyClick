import {StyleSheet} from 'react-native';

export const notifyStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 30,
    flex: 1
  },
  imageContainer: {
    width: 300,
    height: 300,
    borderRadius: 8
  }
});
