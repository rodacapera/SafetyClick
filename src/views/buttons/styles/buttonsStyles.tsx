import {StyleSheet} from 'react-native';

export const buttonsStyles = StyleSheet.create({
  imageContainer: {
    width: 300,
    height: 300,
    borderRadius: 8
  },
  container: {
    alignItems: 'center',
    padding: 30,
    flex: 1
  }
});
